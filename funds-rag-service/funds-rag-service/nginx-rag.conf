# Добавить в существующий серверный блок crackthedeck
# или создать отдельный location блок

# Вариант 1: как часть существующего домена crackthedeck
location /api/rag/ {
    proxy_pass http://127.0.0.1:8100;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 30s;
}

# Вариант 2: отдельный сервер на поддомене (например rag.crackthedeck.com)
# server {
#     listen 80;
#     server_name rag.crackthedeck.com;
#
#     location / {
#         proxy_pass http://127.0.0.1:8100;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
